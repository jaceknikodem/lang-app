#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Type checking
echo "📝 Type checking..."
npm run lint || {
  echo "❌ Type checking failed. Please fix TypeScript errors."
  exit 1
}

# Unit tests
echo "🧪 Running unit tests..."
npm run test:unit || {
  echo "❌ Unit tests failed. Please fix failing tests."
  exit 1
}

# Build check
echo "🔨 Build check..."
npm run build || {
  echo "❌ Build failed. Please fix build errors."
  exit 1
}

echo "✅ Pre-commit checks passed!"
echo "💡 Run 'npm run test:e2e' to test functionality before pushing"
